import 'package:flutter/material.dart';

class AppColors {
  static const purple = Colors.deepPurple;
  static const white = Colors.white;
  static const navyBlue = Color.fromRGBO(39, 58, 70, 1.0);
  static const transparentGray = Color.fromRGBO(255, 255, 255, 0.4);
  static const misty = Color.fromRGBO(250, 250, 250, 1);

  // Not used at the moment
  static const lightPurple = Colors.deepPurpleAccent;
  static const lightGreen = Colors.greenAccent;
  static const cyan = Colors.cyan;
}
